import { useEffect } from 'react';
import { getDash } from '../services/project-api';

const DashBoard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default DashBoard;
